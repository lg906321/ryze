spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  http:
    encoding:
      charset: utf-8
  aop:
    auto: true
  datasource:
    url: jdbc:mysql://localhost:3306/ryze
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  redis:
    port: 6379
server:
  context-path: /ryze
  port: 80
